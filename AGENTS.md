# Agent guidance

## Fork direction

Treat `jonocairns/obsidian-sync-mcp` as the canonical product repository and release line. Upstream contributions are optional and must stay narrowly scoped; upstream acceptance is never a release gate. Prefer fork-first delivery, and selectively pull only focused upstream fixes that clearly improve the fork without displacing its roadmap.
